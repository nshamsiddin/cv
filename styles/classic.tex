% styles/classic.tex — the default look.
% All commands and styling are defined in cv.cls; this template adds no
% overrides. It exists so `\documentclass[classic]{cv}` resolves cleanly and
% so the base can always \input a template file.
\endinput
